import java.awt.*;
import javax.swing.*;

public class FilterButton extends ViewButton {
    Boolean clicked;

    public FilterButton(String text, String color) {
        super(text, color);

        this.addActionListener(e -> {

        });
    }
}
